import "../GlitchText.css"; // Import the generated CSS file

const GlitchText = () => {
  return (
    <div className="content">
      <div className="text" data-text="Frontend Developer">
        Frontend Developer
      </div>
    </div>
  );
};

export default GlitchText;
